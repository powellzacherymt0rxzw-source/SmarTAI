# OmniDocBench OCR 数据处理与测试计划

- 状态：执行中（人工端到端、manifest、Smoke dry-run 与真实运行链路均已通过）
- 目标分支：`xy-dev`
- 计划日期：2026-07-23
- 原则：先验证小样本与成本，再扩大范围；本计划获批前不发起批量 OCR 请求。

## 1. 当前结论

### 1.1 OCR 链路

当前 OCR 链路已经具备首轮测试条件：

- 图片、扫描 PDF、压缩包内图片均已接入视觉 OCR。
- 原生文本和可提取文本的 PDF 会跳过 OCR。
- 当前视觉 provider 可被注册表选中，真实图片调用已成功。
- OCR 相关定向测试为 `13 passed`。

这只能说明“链路可用”，尚不能说明识别精度、稳定性、时延和成本已经达标。

### 1.2 OmniDocBench 适用性

OmniDocBench 可以用于测试当前 OCR，尤其适合：

- 整页图片到 Markdown 的端到端识别；
- 中英文及中英混排；
- 试卷、笔记、模糊扫描；
- 公式、表格、复杂布局和阅读顺序；
- 按来源、语言、布局、困难子集做分层统计。

边界：

- 它是通用文档解析基准，不是专门的学生答卷数据集。
- 数据中有 118 页 `note`，但显式 `handwriting` 特征仅 2 页，不能代表真实手写答题场景。
- 因此它可作为通用 OCR 与试卷近似基准，后续仍需补充内部真实答卷集。
- 数据集仅允许研究、非商业使用，不进入产品数据、不重新分发。

官方资料：

- https://github.com/opendatalab/OmniDocBench/tree/main
- https://huggingface.co/datasets/opendatalab/OmniDocBench

## 2. 固定的数据快照

本地目录：

`data/benchmarks/OmniDocBench`

固定版本：

`aa1ee96d106dbe53d0ae59474d75c6e6d9b53fec`

完整性信息：

- 总体积：约 1.5 GiB；
- 标注页面：1651；
- 图片：1651，缺失 0，多余 0；
- 图片类型：981 张 JPG、670 张 PNG；
- 实际图片内容：1015 张 JPEG、636 张 PNG；
- 34 张文件扩展名为 `.png`、实际内容为 JPEG，manifest 已显式标记；
- 标注 JSON SHA-256：`a45cd84b04ad8b793e775089640e6b681209abea33ead54c1828ddca35fae496`；
- 最大图片：20,267,913 bytes；
- 有 8 张图片超过当前产品的 10 MiB 单图限制；
- 数据目录已加入 `.gitignore`，不会提交到仓库。

主要数据分布：

- `exam_paper`：193 页；
- `note`：118 页；
- `fuzzy_scan`：30 页；
- 简体中文：765 页；
- 英文：755 页；
- 中英混排：116 页；
- `equation_hard`：100 页；
- `layout_hard`：99 页；
- `table_hard`：97 页。

## 3. 测试目标

回答以下问题：

1. 当前产品入口能否稳定处理支持范围内的图片和扫描 PDF？
2. 对试卷、笔记、中英文、公式、表格和复杂布局的识别质量分别如何？
3. 当前 10 MiB 限制、重试和并发配置会造成多少真实失败？
4. 每页时延、重试率和模型成本是否可接受？
5. OCR 错误会如何影响后续的题目解析、学生作答解析和评分流程？

本轮不做：

- 不直接跑完整 1651 页；
- 不把 OmniDocBench 成绩当作真实手写答卷成绩；
- 不为了跑分绕开现有产品链路；
- 不在基准脚本中自动修改业务代码；
- 不在未确认预算时比较大量模型或 provider。

## 4. 数据处理方案

### 4.1 生成可复现 manifest

实现 `tools/ocr_benchmark/prepare_omnidocbench.py`，读取官方 JSON，输出固定顺序的 `manifest.jsonl`。每条记录至少包含：

- `sample_id`、原始图片相对路径和 SHA-256；
- 文件大小、图片格式、宽高；
- `data_source`、`language`、`layout`；
- `special_issue`、困难子集；
- OCR `purpose`；
- 所属测试集合及抽样原因。

准备阶段必须校验：

- 图片可解码；
- 标注与图片一一对应；
- sample id 唯一；
- 抽样结果在相同 seed 下完全一致。

固定随机种子：`20260723`。

### 4.2 两条输入轨

**A. 原始产品轨**

- 原图不做任何变换；
- 走现有 `extract_text_from_upload` 和 `LLMVisionOCRSkill`；
- 保留当前 10 MiB 限制；
- 8 张超限图片应记录为“预期输入拒绝”，不能混入模型故障。

该轨回答“用户今天直接上传会发生什么”。

**B. 规范化分析轨**

- 仅处理原始产品轨无法接收的超限图片；
- 使用固定、可记录的缩放/编码参数将图片降到限制以内；
- 保留原图和转换后图片的哈希、尺寸及参数；
- 结果与原始产品轨分开汇报。

该轨回答“如果完善上传前处理，模型本身能否识别”。

### 4.3 purpose 映射

- `exam_paper` 使用 `problems`；
- `note` 使用 `submissions`；
- 其他文档使用 `reference`；
- 映射写入 manifest 和结果，不在运行时隐式猜测。

## 5. 分阶段样本

### 阶段 M：人工端到端验收，3 组

在自动化 Smoke 之前，先使用：

`manual_test_samples/omnidocbench`

完成人工产品流程验证。每组样例创建独立任务，依次执行：

1. 上传配套题目 Markdown；
2. 校对题干和评分标准；
3. 上传对应手写作答图片；
4. 保存未修改的 OCR 预览并直接批改，得到 Raw 结果；
5. 复制任务，在人工修正 OCR 后再次批改，得到 Corrected 结果；
6. 对比两条结果，区分 OCR、题号映射和评分模型问题。

三组样例分别覆盖：

- 英文数学真实手写答卷与明确错误；
- 中文手写公式和负号、分式；
- 中文密集手写文本与要点评分。

人工验收重点是完整链路、错误归因和评分解释，不以三张图片的平均分作为模型准确率结论。

### 阶段 A：Smoke，12 页

覆盖 12 个互不重复页面：

- 4 页试卷；
- 3 页笔记；
- 2 页模糊扫描；
- 1 页 `equation_hard`；
- 1 页 `table_hard`；
- 1 页 `layout_hard`。

同时覆盖中文、英文、中英混排、JPG、PNG，并至少包含 1 张超过 10 MiB 的图片，以验证两条输入轨。

通过条件：

- 所有可接收页面均完成且结果非空；
- 没有未捕获异常、结果串页或重复计费；
- 超限图片在原始轨得到明确、可解释的拒绝；
- 规范化轨能够继续处理该超限图片；
- 产物可断点续跑。

### 阶段 B：Pilot，120 页

按以下目标分层抽样：

- 试卷 48 页；
- 笔记 36 页；
- 模糊扫描 12 页；
- 困难样本 24 页：公式、表格、布局各 8 页。

规则：

- 分层内尽量平衡中文、英文和中英混排；
- 先去重；重叠样本按“困难样本 > 模糊扫描 > 试卷/笔记”归组；
- 缺口从同层候选中按固定 seed 回填；
- 抽样清单提交评审，之后不因结果好坏更换样本。

Pilot 的目标是建立基线，不预设缺乏证据的精度门槛。完成后根据下游容错要求确定发布阈值。

运行门槛：

- 支持范围内页面完成率不低于 98%；
- 结果、错误、时延和重试信息记录率为 100%；
- 无样本错配和不可解释的跳过；
- Smoke 发现的阻断级问题已解决或有明确豁免。

### 阶段 C：Full，1651 页

仅在以下条件满足后执行：

- Pilot 报告已评审；
- 单页成本和全量预算已确认；
- 运行时长可接受；
- 许可边界再次确认；
- 用户明确批准全量模型调用。

## 6. OCR 执行器

实现 `tools/ocr_benchmark/run_omnidocbench.py`：

- 直接复用现有产品 OCR skill，不复制 provider 调用逻辑；
- 默认并发为 2，与当前配置一致；
- 指数退避并限制最大重试次数；
- 以“图片哈希 + provider/model + prompt/purpose + 参数”作为缓存键；
- 已成功样本不重复请求，失败样本可选择性续跑；
- 每页输出与图片同名的 `.md`，兼容官方 end-to-end evaluator；
- 另写一份 `results.jsonl`，记录运行元数据。

每条结果至少记录：

- sample id、输入/输出哈希；
- provider、model、purpose、prompt 版本；
- 开始时间、耗时、重试次数；
- 状态、错误类型、错误信息；
- 输出字符数、空输出标记、`[unclear]` 次数；
- 可获得时记录输入/输出 token 和估算成本。

任何日志均不得写 API key、请求头或原始凭据。

## 7. 评测方案

### 7.1 官方端到端评测

优先使用 OmniDocBench 官方 `end2end` 流程和 `quick_match`：

- 文本：官方 normalized edit distance，并补充中文/英文 CER；
- 公式：官方公式指标；
- 表格：TEDS；
- 阅读顺序：官方 reading-order 指标；
- 保留官方生成的明细，避免只看一个总分。

### 7.2 产品运行指标

- 成功、空输出、预期拒绝、模型错误和系统错误比例；
- p50、p95、最大单页时延；
- 重试率和重试后恢复率；
- `[unclear]` 出现率；
- 单页及全阶段成本；
- 超限图片对可用率的实际影响。

### 7.3 分层报告

至少按以下维度拆分，避免平均值掩盖问题：

- 来源：`exam_paper`、`note`、其他；
- 语言：简中、英文、中英混排；
- 布局：single、double、three、1andmore、other；
- 图像问题：fuzzy scan、超限图片；
- 困难集：equation、table、layout。

### 7.4 下游回归

对 Smoke 和 Pilot 中的试卷/笔记样本追加下游检查：

- OCR Markdown 能否被题目/作答解析器消费；
- 题号、选项、公式和答案区域是否发生错位；
- OCR 成功但下游失败时，错误必须归因到正确阶段。

这部分只做观测和归因，不在同一批次自动修复业务逻辑。

## 8. 人工误差分析

从 Pilot 中固定抽取至少 24 页人工复核，覆盖：

- 漏字/漏块；
- 幻觉和擅自补全；
- 阅读顺序错误；
- 公式结构错误；
- 表格结构错误；
- 模糊、手写近似样本错误。

每类给出代表案例、严重程度、是否影响下游，以及建议修复位置：

- prompt；
- 图片预处理；
- provider/model；
- OCR 后处理；
- 下游解析器。

## 9. 交付物

计划获批后新增：

- `tools/ocr_benchmark/prepare_omnidocbench.py`
- `tools/ocr_benchmark/run_omnidocbench.py`
- `tools/ocr_benchmark/config/omnidocbench.yaml`
- 对应单元测试；
- 可复现的 sample manifest；
- 被 Git 忽略的预测、缓存和官方评测中间产物；
- 一份包含总览、分层指标、成本、时延和错误案例的 Markdown 报告。

## 10. 建议执行顺序

1. 使用 `manual_test_samples/omnidocbench` 完成 3 组人工端到端评分；
2. 分别记录 Raw 与 Corrected 结果，定位 OCR、题号映射和评分问题；
3. 修复人工流程中的阻断问题或记录明确豁免；
4. 实现并测试 manifest 生成器；
5. 提交 12 页 Smoke 清单供确认；
6. 执行 Smoke，给出实测单页成本和问题清单；
7. 修复自动化 Smoke 的阻断问题或记录豁免；
8. 提交固定的 120 页 Pilot 清单；
9. 经确认后执行 Pilot；
10. 评审准确率、稳定性、时延和成本；
11. 再决定是否运行 1651 页全量测试及补充真实手写答卷集。
