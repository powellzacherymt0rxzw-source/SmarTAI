# ─── Web framework ─────────────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn>=0.30,<1.0
python-multipart>=0.0.20,<1.0
httpx>=0.28,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.6,<3.0
cryptography>=43.0,<47.0
boto3>=1.35,<2.0

# Database / migrations
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0

# ─── LLM providers (langchain wrappers, no top-level langchain needed) ────
langchain-core>=0.3,<0.4
langchain-openai>=0.3,<0.4
langchain-google-genai>=2.1,<3.0
langchain-anthropic>=0.3,<0.4

# ─── Auth ──────────────────────────────────────────────────────────────────
PyJWT>=2.10,<3.0
bcrypt>=5.0,<6.0

# ─── File / archive processing ─────────────────────────────────────────────
rarfile>=4.0,<5.0
py7zr>=1.0,<2.0
PyMuPDF>=1.23,<2.0
python-docx>=1.1,<2.0
python-pptx>=1.0,<2.0

# ─── Compute / RAG MVP ─────────────────────────────────────────────────────
sympy>=1.14,<2.0
numpy>=2.0,<3.0
rank-bm25>=0.2.2,<0.3

# ─── Resilience ────────────────────────────────────────────────────────────
tenacity>=9.0,<10.0

# ─── Health endpoint ───────────────────────────────────────────────────────
psutil>=7.0,<8.0
